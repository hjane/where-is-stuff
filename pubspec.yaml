name: where_is_stuff
description: "收纳助手 - 智能收纳管理应用"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 云开发 SDK
  cloudbase_flutter: ^1.0.3

  # 本地存储
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.0.0

  # 状态管理
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
